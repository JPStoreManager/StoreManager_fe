# # .github/workflows/ci-cd.yml

# name: Dev CI/CD for React App

# on:
#   pull_request:
#     types: [closed]
#     branches: 
#     - 'develop'

# # 환경 변수 (본인 환경에 맞게 수정)
# env:
#   AWS_REGION: ap-northeast-2 # 예: AWS 리전
#   ECR_REGISTRY: <YOUR_AWS_ACCOUNT_ID>.dkr.ecr.ap-northeast-2.amazonaws.com # 예: ECR 레지스트리 주소
#   BASE_IMAGE_REPO: my-react-base # Base 이미지 ECR 리포지토리 이름
#   APP_IMAGE_REPO: my-react-app   # App 이미지 ECR 리포지토리 이름

# jobs:
#   # 1. 파일 변경 감지 작업
#   check-dependency-change:
#     runs-on: ubuntu-latest
#     outputs:
#       is_dependency_changed: ${{ steps.check-dpdcy-change.outputs.is_dependency_changed }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: check if package.json changed
#         id: check-dpdcy-change
#         run: |
#           # 원격 branch와 동기화
#           git fetch origin ${{ github.event.pull_request.base.ref }}
#           # PR에서 package.json이 변경되었는지 확인
#           git diff --name-only origin/${{ github.event.pull_request.base.ref }} > changed_files.txt
#           if grep -q "package.json" changed_files.txt; then
#             echo "is_dependency_changed=true" >> $GITHUB_OUTPUT
#           else
#             echo "is_dependency_changed=false" >> $GITHUB_OUTPUT
#           fi

#   # 2. Base 이미지 빌드 및 푸시 작업
#   # 이 작업은 'check-dependency-change' 작업이 성공적으로 완료되고, package.json 파일이 변경된 경우에만 실행됩니다.
#   build-base-image:
#     runs-on: ubuntu-latest
#     needs: check-dependency-change
#     if: needs.check-dependency-change.outputs.is_dependency_changed == 'true'
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/login-ecr@v1

#       - name: Build, tag, and push base image to Amazon ECR
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#         run: |
#           docker build -t $ECR_REGISTRY/${{ env.BASE_IMAGE_REPO }}:latest -f Dockerfile.base .
#           docker push $ECR_REGISTRY/${{ env.BASE_IMAGE_REPO }}:latest
  
#   # 3. App 이미지 빌드 및 배포 작업
#   build-and-deploy-app:
#     runs-on: ubuntu-latest
#     needs: build-base-image # 'build-base-image' 작업이 완료된 후 실행 (성공/실패/건너뛰기 모두 포함)
#     if: always() # Base 이미지 빌드 작업 결과와 상관없이 항상 실행
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/login-ecr@v1

#       - name: Build, tag, and push app image to Amazon ECR
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }} # 커밋 해시를 이미지 태그로 사용
#         run: |
#           # Dockerfile.app 내의 ECR 주소를 실제 주소로 치환하여 빌드 준비
#           sed -i "s|<YOUR_AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/${{ env.BASE_IMAGE_REPO }}>|${{ env.ECR_REGISTRY }}/${{ env.BASE_IMAGE_REPO }}|g" Dockerfile.app
          
#           docker build -t $ECR_REGISTRY/${{ env.APP_IMAGE_REPO }}:$IMAGE_TAG -f Dockerfile.app .
#           docker build -t $ECR_REGISTRY/${{ env.APP_IMAGE_REPO }}:latest -f Dockerfile.app .
          
#           docker push $ECR_REGISTRY/${{ env.APP_IMAGE_REPO }}:$IMAGE_TAG
#           docker push $ECR_REGISTRY/${{ env.APP_IMAGE_REPO }}:latest

#       - name: Create CodeDeploy Deployment
#         id: deploy
#         run: |
#           aws deploy create-deployment \
#             --application-name MyReactApp \
#             --deployment-group-name MyReactApp-Deployment-Group \
#             --revision '{"revisionType":"S3","s3Location":{"bucket":"<YOUR_S3_BUCKET>","key":"appspec.yml","bundleType":"YAML"}}' \
#             --deployment-config-name CodeDeployDefault.OneAtATime \
#             --description "Deploying commit ${{ github.sha }}"